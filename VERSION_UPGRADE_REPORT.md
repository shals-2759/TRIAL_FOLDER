# 🔄 Dependency Upgrade Report

_Auto-generated by Version Change Analyzer on 2026-07-01 11:53 UTC_

## Summary

| Dependency | Ecosystem | Current | Latest | Risk | Breaking Changes |
|---|---|---|---|---|---|
| `hashicorp/azurerm` | terraform | `~> 4.0` | `4.79.0` | **LOW** | 0 |
| `actions/checkout` | github_actions | `v4` | `v7.0.0` | **HIGH** | 5 |
| `actions/checkout` | github_actions | `v4` | `v7.0.0` | **HIGH** | 4 |

---

## `hashicorp/azurerm`

**Summary:** This upgrade to version 4.79.0 introduces several new resources and data sources related to NetApp volume buckets and SignalR service custom certificates. It also includes enhancements for existing resources, such as increased limits for data protection storage containers, new PostgreSQL Flexible Server SKUs, and scale unit support for Virtual Network Gateways. No explicit breaking changes are noted in this version.

### ✨ New Features

- New Action: azurerm_netapp_volume_bucket_credentials
- New Data Source: azurerm_netapp_volume_bucket
- New Data Source: azurerm_netapp_volume_bucket_with_server
- New List Resource: azurerm_netapp_volume_bucket
- New List Resource: azurerm_netapp_volume_bucket_with_server
- New List Resource: azurerm_signalr_service_custom_certificate
- New Resource: azurerm_netapp_volume_bucket
- New Resource: azurerm_netapp_volume_bucket_with_server
- azurerm_data_protection_backup_instance_data_lake_storage now supports increasing the limit of items for 'storage_container_names' from 100 to 1000
- azurerm_postgresql_flexible_server now supports Ddsv6 skus in validation
- azurerm_virtual_network_gateway now supports 'maximum_scale_unit' and 'minimum_scale_unit'

## `actions/checkout`

**Summary:** This upgrade from actions/checkout v4 to v7.0.0 introduces several critical breaking changes, primarily around runner version requirements (Node.js 24, minimum v2.327.1, and v2.329.0 for specific Docker scenarios) and a significant security hardening that blocks checking out fork PRs in `pull_request_target` and `workflow_run` events. It also adds new features such as partial checkout filters, an SSH user parameter, and improved multiple repository checkout capabilities.

### ⚠️ Breaking Changes

- **[CRITICAL]** The action now requires a runner environment that supports Node.js 24, affecting self-hosted runners.
- **[CRITICAL]** Minimum compatible runner version is now v2.327.1. Workflows on older self-hosted runners will fail.
- **[WARNING]** Credential persistence behavior changed to store credentials under $RUNNER_TEMP instead of directly in the local git config. This change primarily affects Docker container actions that might rely on the previous credential storage location.
- **[CRITICAL]** Minimum compatible runner version for Docker container actions using credential persistence is now v2.329.0.
- **[CRITICAL]** Checking out fork pull requests is now blocked for workflows triggered by `pull_request_target` and `workflow_run` events due to security hardening. Workflows attempting this will fail.

### ✨ New Features

- Added support for partial checkout filters.
- Added a new SSH user parameter.
- Introduced `Ref` and `Commit` outputs.
- Added support for checking out multiple internal repositories.
- Enhanced credential persistence with worktree support for `includeIf`.
- Added `orchestration_id` to the git user-agent when `ACTIONS_ORCHESTRATION_ID` is set.

### 🛠 Migration Steps

1. Ensure all self-hosted runners are updated to an environment that supports Node.js 24.
2. Upgrade self-hosted GitHub Actions runners to at least version v2.327.1.
3. Review workflows using credential persistence, especially within Docker container actions, and ensure self-hosted runners are updated to at least v2.329.0.
4. Evaluate workflows using `actions/checkout` with `pull_request_target` or `workflow_run` events on fork repositories. Adjust workflow logic if attempting to checkout fork code, as this is now blocked for security reasons; consider alternative strategies for accessing fork content if necessary.

## `actions/checkout`

**Summary:** The upgrade to actions/checkout v7.0.0 introduces critical changes requiring runner updates to at least v2.329.0 for full compatibility, especially for Docker container actions. It also implements a security block against checking out fork PRs in 'pull_request_target' and 'workflow_run' events. New capabilities include partial checkout filters, SSH user parameters, 'Ref'/'Commit' outputs, and multi-repository checkout support.

### ⚠️ Breaking Changes

- **[WARNING]** The sparse-checkout functionality might be impacted or disabled on Git versions prior to 2.27.0. While the action attempts to disable it gracefully, workflows relying on this feature with older Git versions may experience functionality loss.
- **[CRITICAL]** The action now runs on Node.js 24, requiring GitHub Actions runners to be updated to a minimum version of v2.327.1.
- **[CRITICAL]** Credentials are now persisted to a separate file under $RUNNER_TEMP instead of directly in the local Git config. This change requires a minimum Actions Runner version of v2.329.0, especially for Docker container actions scenarios.
- **[CRITICAL]** The action now blocks checking out fork pull requests when using the pull_request_target and workflow_run events due to security considerations.

### ✨ New Features

- Added support for partial checkout filters.
- Added an SSH user parameter.
- Added 'Ref' and 'Commit' outputs to the action.
- Added support for checking out multiple internal repositories.
- Added worktree support for 'persist-credentials includeIf'.
- Added orchestration_id to the Git user-agent when ACTIONS_ORCHESTRATION_ID is set.

### 🛠 Migration Steps

1. If your workflow uses sparse-checkout, ensure your runner environment has Git version 2.27.0 or newer to guarantee expected behavior and avoid automatic disabling of the feature.
2. Update all GitHub Actions runners to at least version v2.327.1 to be compatible with the Node.js 24 runtime.
3. If using Docker container actions that rely on persisted credentials, ensure your GitHub Actions runners are updated to at least v2.329.0.
4. Review workflows using 'pull_request_target' or 'workflow_run' events that previously checked out fork PRs, as this functionality is now blocked for security. Adjust your workflow logic to accommodate this restriction or use alternative secure methods for handling fork contributions.
